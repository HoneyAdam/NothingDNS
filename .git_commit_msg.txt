dnsctl: implement missing subcommands (zone export, cluster join/leave, blocklist reload)

zone: implement `zone export` for BIND-format zone export via GET /api/v1/zones/{name}/export

cluster: implement `cluster join` and `cluster leave` subcommands for cluster
membership management. join POSTs to /api/v1/cluster/join, leave DELETEs
to /api/v1/cluster/leave.

blocklist: add `blocklist reload` subcommand that calls /api/v1/config/reload
to reload blocklists and all config.

cache: pass optional name argument to flush to allow targeted cache invalidation.

cluster peers: restore error return when API returns non-array nodes field
(matching the existing test expectation).