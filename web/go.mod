// NOTICE: This go.mod is a leftover from an older project layout.
// The web/ directory is a Vite/React/TypeScript frontend served by
// the Go dashboard in internal/dashboard/. Do not add dependencies
// here — the real Go module root is one level up (go.mod at /).
module github.com/nothingdns/nothingdns/web

go 1.26.5
